# Changelog

## 1.0.0

Initial release. Packages the work-loop post-implementation hardening skill as a
Claude Code plugin. Bundles a single-plugin marketplace that re-lists
`skill-codex` (the OpenAI Codex bridge) sha-pinned, so it auto-installs as a
dependency without the user adding a second marketplace.
